# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env (values like Supabase URL + anon key are public-by-design but kept
# out of git so private secrets never accidentally land here)
.env
.env.*
!.env.example

# Supabase CLI cache (project ref, pooler URL, version pins)
supabase/.temp/

# Terraform / OpenTofu
**/.terraform/
**/.terraform.lock.hcl
**/*.tfstate
**/*.tfstate.*
**/*.tfvars
!**/*.tfvars.example
**/crash.log
